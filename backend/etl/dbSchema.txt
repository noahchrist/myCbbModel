-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

public.daily_snapshots (
  snapshot_id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  created_datetime timestamp with time zone NOT NULL,
  game_id text NOT NULL,
  game_date date NOT NULL,
  home_kp_id integer,
  away_kp_id integer,
  home_team_name text,
  home_score integer,
  away_team_name text,
  away_score integer,
  pt_diff integer,
  pt_total integer,
  is_home boolean,
  is_neutral boolean,
  home_adj_off_eff numeric,
  home_eff_fg_pct numeric,
  home_adj_def_eff numeric,
  home_def_eff_fg_pct numeric,
  home_adj_tempo numeric,
  home_threes_pct numeric,
  home_threes_rate numeric,
  home_ft_rate numeric,
  home_ft_pct numeric,
  home_def_ft_rate numeric,
  home_block_pct numeric,
  home_opp_threes_pct numeric,
  home_opp_threes_rate numeric,
  home_stl_rate numeric,
  home_non_stl_trnvr_rate numeric,
  home_off_reb_pct numeric,
  home_def_reb_pct numeric,
  home_ast_rate numeric,
  home_trnvr_pct numeric,
  home_eff_height numeric,
  home_exp_rtg numeric,
  home_bench_rtg numeric,
  home_cont_rtg numeric,
  away_adj_off_eff numeric,
  away_eff_fg_pct numeric,
  away_adj_def_eff numeric,
  away_def_eff_fg_pct numeric,
  away_adj_tempo numeric,
  away_threes_pct numeric,
  away_threes_rate numeric,
  away_ft_rate numeric,
  away_ft_pct numeric,
  away_def_ft_rate numeric,
  away_block_pct numeric,
  away_opp_threes_pct numeric,
  away_opp_threes_rate numeric,
  away_stl_rate numeric,
  away_non_stl_trnvr_rate numeric,
  away_off_reb_pct numeric,
  away_def_reb_pct numeric,
  away_ast_rate numeric,
  away_trnvr_pct numeric,
  away_eff_height numeric,
  away_exp_rtg numeric,
  away_bench_rtg numeric,
  away_cont_rtg numeric,
  CONSTRAINT daily_snapshots_pkey PRIMARY KEY (snapshot_id)
);
public.games (
  game_id text NOT NULL UNIQUE,
  created_datetime timestamp with time zone NOT NULL,
  commence_datetime timestamp with time zone,
  game_date date NOT NULL,
  is_completed boolean,
  home_kp_id integer,
  away_kp_id integer,
  home_team_name text,
  home_score integer,
  away_team_name text,
  away_score integer,
  pt_diff integer,
  pt_total integer,
  fd_home_hh_price integer,
  fd_away_hh_price integer,
  fd_home_spread numeric,
  fd_home_spread_price integer,
  fd_away_spread numeric,
  fd_away_spread_price integer,
  fd_over numeric,
  fd_over_price integer,
  fd_under numeric,
  fd_under_price integer,
  CONSTRAINT games_pkey PRIMARY KEY (game_id)
);
public.model_details (
  model_id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  created_datetime timestamp with time zone NOT NULL,
  user_id text NOT NULL,
  model_name text NOT NULL,
  model_seed integer NOT NULL UNIQUE,
  ten_digit text,
  weight_gen_off integer,
  weight_gen_def integer,
  weight_pace integer,
  weight_threes integer,
  weight_fts integer,
  weight_per_def integer,
  weight_int_def integer,
  weight_boards integer,
  weight_playmaking integer,
  weight_intangibles integer,
  total_wl text,
  total_units_bet numeric,
  total_units_won numeric,
  CONSTRAINT model_details_pkey PRIMARY KEY (model_id)
);
public.model_names (
  model_name_id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  model_name text NOT NULL UNIQUE,
  user_id text,
  times_rejected integer,
  CONSTRAINT model_names_pkey PRIMARY KEY (model_name_id)
);
public.model_predictions (
  prediction_id integer GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  created_datetime timestamp with time zone NOT NULL,
  model_id integer NOT NULL,
  user_id text NOT NULL,
  game_id text NOT NULL,
  game_date date NOT NULL,
  is_completed boolean,
  predicted_pt_diff numeric,
  predicted_pt_total numeric,
  predicted_edge numeric,
  prediction_summary text,
  is_won boolean,
  units_bet numeric,
  units_won numeric,
  CONSTRAINT model_predictions_pkey PRIMARY KEY (prediction_id)
);
public.teams_master (
  kp_id integer NOT NULL UNIQUE,
  team_name text NOT NULL,
  alt_names text,
  theodds_team_name text,
  seasons text,
  CONSTRAINT teams_master_pkey PRIMARY KEY (kp_id)
);



models.py:
create_model
get_model_names
delete_model
get_user_models // get_model_history // get_model_daily_picks (proabably can be combined)

community:
get_community_models
get_todays_top_picks // get_top_picks_performance

